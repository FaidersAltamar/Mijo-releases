# Mijo releases

Public installers and auto-update manifests for Mijo.

This repository contains **release assets only** — no application source code. Desktop auto-update (`electron-updater`) and download pages point here.

Installers are published automatically from the private source repository when a version tag is released.
